Index: b/src/config/libvga.config
===================================================================
--- a/src/config/libvga.config
+++ b/src/config/libvga.config
@@ -243,7 +243,7 @@ kbd_only_root_keymaps
 HorizSync 31.5 35.5
 VertRefresh 50 90
 
-# Montitor timings
+# Monitor timings
 #
 # These are prefered over the default timings (if monitor and chipset
 # can handle them). Not all drivers use them at the moment, and Mach32
